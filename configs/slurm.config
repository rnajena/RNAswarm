process {
    withLabel:  simulate_interactions   { cpus = 1; queue = params.slurm_queue }
    withLabel:  python3                 { cpus = 8; queue = params.slurm_queue }
    withLabel:  preprocessing           { cpus = 8; queue = params.slurm_queue }
    withLabel:  mapping_segemehl        { cpus = params.max_cpus; queue = params.slurm_queue }
    withLabel:  mapping_bwa             { cpus = params.max_cpus; queue = params.slurm_queue }
    withLabel:  mapping_samtools        { cpus = params.max_cpus; queue = params.slurm_queue }
    withLabel:  mapping_hisat2          { cpus = params.max_cpus; queue = params.slurm_queue }
    withLabel:  python2                 { cpus = 1; queue = params.slurm_queue }
    withLabel:  generate_report_multiqc { cpus = 1; queue = params.slurm_queue }
    withLabel:  generate_report_kraken  { cpus = params.max_cpus; queue = params.slurm_queue }
    withLabel:  generate_report_sortmerna { cpus = params.max_cpus; queue = params.slurm_queue }
}